#!/usr/bin/env bash
# Thin wrapper: `tt <args...>` -> `node src/cli.ts <args...>`, resolved
# relative to this script so it works regardless of the caller's cwd.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec node "$DIR/src/cli.ts" "$@"
